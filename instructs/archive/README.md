# Archived Phase Memory

Thư mục này giữ lại toàn bộ phase notes cũ để bảo toàn traceability.

Quy ước sau lượt cleanup:

- Root `instructs/` chỉ nên giữ các file memory hiện hành, gọn và dễ đọc.
- `instructs/archive/` giữ lịch sử chi tiết theo phase.
- Archive hiện giữ toàn bộ phase notes từ `phase_01_*` đến `phase_06_*`.

Thứ tự đọc khuyến nghị:

1. `../PROJECT_MEMORY_MASTER.md`
2. `../OPEN_ITEMS.md`
3. chỉ vào archive khi cần truy lịch sử quyết định theo phase

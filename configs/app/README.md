# App Config Notes

Phase 2 provides environment-driven runtime configuration via `.env.example` and `src/causal_app/config.py`.

- No final app config schema is implemented yet.
- Keep future runtime-oriented config here rather than reusing notebook hardcoded paths.

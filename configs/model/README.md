# Model Config Notes

This directory is reserved for future model configuration once notebook logic is extracted into reusable modules.

- No serving-ready model config exists yet.
- Do not infer canonical model hyperparameter files from the current legacy notebooks.
